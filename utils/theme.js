import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#53dd30",
    },
    secondary: {
      main: "#000000",
    },
    background: {
      default: "#000000",
      paper: "#2f2f2f",
    },
    text: {
      primary: "#ffffff",
    },
  },
});
